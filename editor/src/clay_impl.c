// Clay (nicbarker/clay, zlib) — the single implementation translation unit.
// Compiled AS C (mingw gcc) so the C-linkage symbols match the `extern "C"`
// declarations main.cpp pulls from clay.h. See docs/LAYOUT_ENGINE.md.
#define CLAY_IMPLEMENTATION
#include "clay.h"
